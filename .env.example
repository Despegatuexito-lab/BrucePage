# No se usa en runtime (los datos van a Cloudflare D1 vía driver adapter),
# solo satisface la validación del schema para `prisma generate`/CLI.
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"
